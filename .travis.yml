language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dkbond007/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dkbond007/docker-react npm run test
